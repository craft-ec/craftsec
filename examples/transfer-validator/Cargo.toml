[package]
name = "transfer-validator"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = "s"
lto = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
